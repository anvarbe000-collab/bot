python-telegram-bot[job-queue]>=21.0   # [job-queue] — to'lov muddati (5 daqiqa) avtomatik tugashi uchun
openai>=1.0
python-pptx>=1.0
python-docx>=1.0   # "Matn tuzatish" xizmati uchun (.docx o'qish/yozish)
pypdf>=5.0          # "Davom ettirish" xizmati uchun (.pdf dan matn o'qish)
pillow>=10.0
python-dotenv>=1.0
requests>=2.31
google-genai>=0.3   # faqat Nano banana (gemini) uchun
together>=1.3   # faqat Together (Flux) uchun
aiohttp>=3.9   # Click to'lov tizimi webhook server (Prepare/Complete) uchun
